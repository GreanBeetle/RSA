import React, { useRef } from 'react'
import {
  Animated,
  View, 
  Text, 
  PanResponder
} from 'react-native'

const VideoClip = ({ videoName  }) => <Text>VideoClip</Text>

export default VideoClip